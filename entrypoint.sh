#!/bin/sh

# CHANGE_ME: you can hardcode arguments for your app like below (uncomment if needed):
# berlin=2950159
# To run your app with arguments use the following example
# weather -units="${UNITS:-metric}" -appId="${WEATHERAPPID:-}" -cityId="${CITYID:-$berlin}"

./weather
